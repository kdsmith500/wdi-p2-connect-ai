// var express = require('express');
// var router = express.Router();
// var repliesCtrl = require('../../controllers/api/replies');

// router.get('/:id/replies', repliesCtrl.index);
// router.get('/:id/replies/:reply', repliesCtrl.show);
// router.post('/:id/replies', repliesCtrl.create);

// module.exports = router;